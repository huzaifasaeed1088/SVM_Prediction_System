# -*- coding: utf-8 -*-
"""Untitled15.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/14lNvEUsrPcv4TH6ze79uAnlh_kfdGNmv
"""

import streamlit as st
import joblib
import numpy as np

# Load the trained SVM model
model = joblib.load('svm_model.pkl')

# Streamlit app
st.title("Customer Purchase Prediction App")
st.write("Predict whether a customer will make a purchase based on Gender, Age, and Estimated Salary.")

# Input form
st.sidebar.header("Input Features")
gender = st.sidebar.selectbox("Gender", ("Male", "Female"))
age = st.sidebar.slider("Age", 01, 70, 25)  # Slider for age
salary = st.sidebar.number_input("Estimated Salary", min_value=01, max_value=1000000000000000000, step=1000, value=50000)  # Numeric input for salary

# Preprocess inputs
gender_encoded = 1 if gender == "Male" else 0  # Encode gender as 1 for Male, 0 for Female
input_features = np.array([[gender_encoded, age, salary]])

# Prediction
if st.sidebar.button("Predict"):
    prediction = model.predict(input_features)[0]
    if prediction == 1:
        st.success("The model predicts: **Customer WILL Purchase** 🎉")
    else:
        st.warning("The model predicts: **Customer WILL NOT Purchase**.")

st.sidebar.write("Huzaifa Saeed")
st.sidebar.write("Built with ❤️ using Streamlit")